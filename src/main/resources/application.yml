server:
  port: 8888

spring:
  application:
    name: spring-config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/123VladimiR123/server-config.git
          default-label: main
          search-paths: "{application}"